# Core deep learning framework
torch>=2.2.0
torchaudio>=2.2.0

# Foundation backbones & codecs
transformers>=4.52.0
accelerate>=0.34.0
peft>=0.12.0
snac
bitsandbytes

# Local dialogue generation (Milestone 1) via Qwen2.5-Omni — replaces the Gemini/OpenAI API
qwen-omni-utils
json-repair

# Model export & runtime engine
onnx>=1.16.0
onnxruntime-gpu>=1.19.0
optimum[onnxruntime]>=1.22.0
onnxruntime-genai

# Audio processing & augmentation
librosa>=0.10.2
soundfile>=0.12.1
scipy>=1.13.0
edge-tts

# Telemetry & security simulation
cryptography>=42.0.0
numpy>=1.26.0

# Data handling
pandas
pyarrow
python-dotenv

# GitHub Release artifact upload (replaces Google Drive persistence)
requests
